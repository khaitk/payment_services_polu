# Payment Services

#### 1. Build Project

```
docker image build -t payment_services .
```

#### 2. Run Container

```
docker run -p 5000:5000 -d payment_services
```

#### 3. Run localhost

[http://localhost:5000/checkout.html](http://localhost:5000/checkout.html)